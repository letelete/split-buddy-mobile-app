import { StyleProp, ViewStyle } from 'react-native';

/* -------------------------------------------------------------------------------------------------
 * Stylable
 * -----------------------------------------------------------------------------------------------*/

interface Stylable<T = ViewStyle> {
  containerStyle?: StyleProp<T>;
}

/* -------------------------------------------------------------------------------------------------
 * Suspensible
 * -----------------------------------------------------------------------------------------------*/

interface Suspensible {
  loading?: boolean;
}

/* -----------------------------------------------------------------------------------------------*/

export type { Stylable, Suspensible };
